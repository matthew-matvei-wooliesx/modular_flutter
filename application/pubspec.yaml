name: application
description: A new Flutter package project.
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  domain:
    path: ../domain

dev_dependencies:
  lints: ^1.0.0
